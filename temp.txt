"""
CRITICAL PYTHON RULES — VIOLATIONS CAUSE RUNTIME ERRORS:
- All Chart.js code lives INSIDE a Python string (the html variable). It is NOT executed as Python.
- Never use JavaScript variable names (`context`, `ctx`, `chart`, `datasets`, `labels`)
  as Python variable names outside the html string.
- The only Python variables available are: `data` (list of dicts), `output_path` (str), `json` (module).
- Extract labels/values in Python like this:
    labels = [row['ColumnName'] for row in data]
    values = [row['NumericCol'] for row in data]
  Then embed with json.dumps():
    html = f'...<script>const labels = {json.dumps(labels)};</script>...'
- Never write `ctx = ...` or `context = ...` in Python — these are JS-only names.
- Never call any function that isn't defined in your own code or in Python builtins.
- The roundTopPlugin must be defined entirely inside the HTML string as JavaScript.
  Do NOT attempt to write it as Python code.
"""
