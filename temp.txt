/* ── RESPONSIVE OVERRIDES ── */
@media (max-width: 768px) {
  /* Sidebar collapsed state */
  .sidebar.collapsed {
    transform: translateX(-100%);
    box-shadow: none;
  }
  
  /* Hamburger toggle button */
  .sidebar-toggle {
    display: flex !important;
    position: fixed;
    top: 12px;
    left: 12px;
    z-index: 2000;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--surface);
    border: 1px solid var(--border);
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    transition: transform 0.2s, background 0.2s;
  }
  .sidebar-toggle:hover { background: var(--slate-50); transform: scale(1.05); }
  .sidebar-toggle svg { width: 20px; height: 20px; color: var(--text-secondary); }

  /* Topbar adjustments */
  .topbar {
    padding: 0 16px 0 60px; /* space for hamburger */
    height: 52px;
    min-height: 52px;
    flex-wrap: wrap;
    gap: 8px;
  }
  .topbar-title {
    font-size: 13px;
    flex: 1 1 100%;
    order: 3;
    margin-top: 4px;
    text-align: center;
  }
  .topbar-badge, .token-balance-wrap {
    order: 1;
    flex: 0 0 auto;
  }
  .token-balance-wrap {
    order: 2;
  }

  /* Input area fixed at bottom */
  .input-area {
    position: fixed !important;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--surface);
    border-top: 1px solid var(--border);
    padding: 12px 16px 16px;
    z-index: 1000;
    box-shadow: 0 -2px 12px rgba(0,0,0,0.04);
  }
  .input-wrap {
    border-radius: var(--radius-lg);
    padding: 8px 10px 8px 14px;
  }
  #query-input {
    font-size: 15px;
    min-height: 24px;
    max-height: 100px;
  }
  .send-btn {
    width: 38px;
    height: 38px;
  }
  .input-hint {
    font-size: 10px;
    margin-top: 6px;
  }

  /* Chat area padding for fixed input */
  .chat-area {
    padding-bottom: 140px !important; /* input height + margin */
    padding-top: 16px;
  }

  /* Message bubbles */
  .msg-row {
    padding: 8px 16px;
    gap: 10px;
  }
  .bubble {
    font-size: 14px;
    padding: 12px 14px;
    border-radius: var(--radius-md);
  }
  .bubble.user-bubble {
    border-radius: var(--radius-md) var(--radius-md) 4px var(--radius-md);
  }
  .bubble.ai-bubble {
    border-radius: 4px var(--radius-md) var(--radius-md) var(--radius-md);
  }
  .bubble-wrap {
    max-width: 100%;
  }
  .msg-row.user .bubble-wrap {
    max-width: 85%;
  }

  /* Thinking block mobile */
  .thinking-header {
    font-size: 12px;
    padding: 8px 12px;
  }
  .thinking-step {
    font-size: 12px;
    padding: 6px 0;
  }
  .step-icon {
    width: 16px;
    height: 16px;
    font-size: 9px;
  }

  /* Meta grid stacks on mobile */
  .meta-grid {
    grid-template-columns: 1fr;
    gap: 6px;
  }

  /* Chart iframe height */
  .chart-iframe {
    height: 320px;
  }

  /* Sidebar actions stack */
  .sidebar-actions {
    flex-direction: column;
    gap: 8px;
    padding: 12px;
    height: auto;
  }
  .sidebar-action-btn {
    width: 100%;
    justify-content: center;
  }

  /* Token info bubble reposition */
  .token-info-bubble {
    right: auto;
    left: 0;
    top: calc(100% + 8px);
    min-width: 200px;
  }
  .tib-arrow {
    right: auto;
    left: 16px;
  }

  /* Quota notices full width */
  .quota-blocked-notice,
  .quota-warn-toast {
    margin: 0 -16px 12px;
    border-radius: 0;
    padding: 10px 16px;
  }
}

/* Tablet range */
@media (min-width: 769px) and (max-width: 1024px) {
  .sidebar { width: 200px; min-width: 200px; }
  .topbar { padding: 0 20px; }
  .chat-area { padding-bottom: 0; }
  .input-area { position: static; padding: 16px 24px 20px; }
}

/* Desktop: ensure sidebar toggle hidden */
@media (min-width: 1025px) {
  .sidebar-toggle { display: none !important; }
  .sidebar { transform: translateX(0) !important; }
}

/* ── Smooth transitions for sidebar toggle ── */
.sidebar {
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
              box-shadow 0.3s ease;
  will-change: transform;
}

/* Prevent layout shift when sidebar collapses */
.main {
  transition: margin-left 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.sidebar.collapsed + .main {
  margin-left: 0;
}

/* ── Touch-friendly improvements ── */
@media (hover: none) and (pointer: coarse) {
  .send-btn, .sidebar-toggle, .chart-open-btn,
  .sql-copy-btn, .copy-prompt-btn {
    min-width: 44px;
    min-height: 44px;
    padding: 8px;
  }
  .history-item, .sidebar-action-btn {
    min-height: 44px;
    padding: 10px 12px;
  }
}


----------------




<!-- Hamburger toggle button (mobile only) -->
<button class="sidebar-toggle" id="sidebar-toggle" onclick="toggleSidebar()" title="Toggle sidebar" style="display:none;">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <line x1="3" y1="12" x2="21" y2="12"></line>
    <line x1="3" y1="6" x2="21" y2="6"></line>
    <line x1="3" y1="18" x2="21" y2="18"></line>
  </svg>
</button>


--------------


// ── Sidebar Toggle Logic ──
function toggleSidebar() {
  const sidebar = document.querySelector('.sidebar');
  const toggleBtn = document.getElementById('sidebar-toggle');
  const isCollapsed = sidebar.classList.toggle('collapsed');
  
  // Update hamburger icon
  if (toggleBtn) {
    toggleBtn.innerHTML = isCollapsed 
      ? `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5M12 5l-7 7 7 7"/></svg>`
      : `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/></svg>`;
  }
  
  // Save preference
  localStorage.setItem('sidebarCollapsed', isCollapsed ? '1' : '0');
  
  // Re-flow chat area
  setTimeout(() => {
    const area = document.getElementById('chat-area');
    if (area) area.scrollTop = area.scrollHeight;
  }, 300);
}

// Init sidebar state on load
document.addEventListener('DOMContentLoaded', () => {
  // Show toggle button on mobile
  if (window.innerWidth <= 768) {
    document.getElementById('sidebar-toggle').style.display = 'flex';
  }
  
  // Restore saved state
  const saved = localStorage.getItem('sidebarCollapsed');
  if (saved === '1' && window.innerWidth <= 768) {
    document.querySelector('.sidebar')?.classList.add('collapsed');
    const btn = document.getElementById('sidebar-toggle');
    if (btn) btn.innerHTML = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5M12 5l-7 7 7 7"/></svg>`;
  }
  
  // Handle resize
  window.addEventListener('resize', () => {
    const toggle = document.getElementById('sidebar-toggle');
    if (window.innerWidth <= 768) {
      if (toggle) toggle.style.display = 'flex';
    } else {
      if (toggle) toggle.style.display = 'none';
      document.querySelector('.sidebar')?.classList.remove('collapsed');
    }
  });
});

// Close sidebar when clicking outside on mobile (optional)
document.addEventListener('click', (e) => {
  if (window.innerWidth > 768) return;
  const sidebar = document.querySelector('.sidebar');
  const toggle = document.getElementById('sidebar-toggle');
  if (!sidebar || !toggle) return;
  
  if (sidebar.classList.contains('collapsed') && 
      !sidebar.contains(e.target) && 
      !toggle.contains(e.target)) {
    // Optional: auto-close when clicking chat area
    // toggleSidebar();
  }
});
