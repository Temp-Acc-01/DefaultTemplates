@media (max-width: 700px) {

  .topbar {

    display: flex;
    align-items: center;

    gap: 8px;

    padding: 10px 12px;

    flex-wrap: nowrap;

    overflow: hidden;
  }

  .sidebar-toggle {
    flex-shrink: 0;
  }

  .topbar-title {

    flex: 1;

    min-width: 0;

    font-size: 13px;

    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* KPI group */
  .topbar-badge,
  .token-balance-wrap {

    flex-shrink: 0;
  }

  .topbar-badge {

    font-size: 9.5px;

    padding: 3px 7px;

    white-space: nowrap;
  }

  .token-pill {

    font-size: 9.5px;

    padding: 3px 7px;

    gap: 3px;

    white-space: nowrap;

    max-width: 110px;

    overflow: hidden;
  }

  /* hide verbose label */
  .token-pill span:first-child {
    display: none;
  }

  #token-text {
    font-size: 10px;
    font-weight: 600;
  }

  .token-info-icon {
    width: 14px;
    height: 14px;
    margin-left: 1px;
  }

  .token-info-icon svg {
    width: 12px;
    height: 12px;
  }
}
