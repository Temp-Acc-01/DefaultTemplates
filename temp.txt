LABEL_INTELLIGENCE_RULES = """
## Intelligent Label & Legend Rules (apply based on data size — never hardcode)

Inspect the data BEFORE writing any chart code, then apply these rules:

### Legend
- If number of unique series/categories > 15  → HIDE the legend entirely (legend: {display: false})
- If 8 < unique series <= 15                  → show legend but position it BOTTOM, font size 10
- If unique series <= 8                       → show legend normally

### X-axis tick labels
- If number of x-axis labels > 20             → rotate 45°, font size 10, max tick rotation
- If number of x-axis labels > 40             → HIDE x-axis tick labels entirely (display: false)
- If number of x-axis labels <= 20            → show normally, rotate 0°

### Y-axis label
- Always display Y-axis title HORIZONTALLY by setting rotation: 0 in ticks config
- Never let the axis title overlap the chart area

### Bar chart with grouped/stacked series > 15 categories
- Automatically switch display to STACKED bar (stacked: true) 
- This collapses the visual noise of 100+ grouped bars

### General
- Always set layout padding: { top: 20, right: 20, bottom: 60, left: 60 }
- Always call chart.resize() after render to fit the container
- Never show a tooltip that displays all 300+ series at once — limit tooltip to top 5 values
"""



Before writing code, state:
- How many rows in the data?
- How many unique X-axis values?  
- How many unique series/legend items?
Then apply the label intelligence rules above.



------------------------


/* ── Chart Modal ── */
.chart-modal-overlay {
  display: none;
  position: fixed; inset: 0;
  background: rgba(15, 25, 50, 0.45);
  backdrop-filter: blur(3px);
  z-index: 1000;
  align-items: center;
  justify-content: center;
  padding: 32px;
  animation: fadeUp 0.2s ease;
}
.chart-modal-overlay.open { display: flex; }

.chart-modal {
  background: var(--surface);
  border: 1px solid var(--border-md);
  border-radius: var(--radius-xl);
  box-shadow: 0 24px 64px rgba(15,25,50,0.18);
  width: 100%;
  max-width: 1100px;
  height: 82vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeUp 0.22s ease;
}

.chart-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 18px 12px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.chart-modal-title {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
  letter-spacing: -0.1px;
}

.chart-modal-close {
  width: 28px; height: 28px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  background: transparent;
  color: var(--text-muted);
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  transition: background 0.12s, color 0.12s, border-color 0.12s;
  font-family: 'DM Sans', sans-serif;
}
.chart-modal-close:hover {
  background: var(--slate-100);
  color: var(--text-primary);
  border-color: var(--border-md);
}
.chart-modal-close svg { width: 14px; height: 14px; }

.chart-modal-body {
  flex: 1;
  overflow: hidden;
  padding: 0;
}
.chart-modal-body iframe {
  width: 100%;
  height: 100%;
  border: none;
  display: block;
}










----------------------


<!-- Chart Modal -->
<div class="chart-modal-overlay" id="chart-modal-overlay" onclick="closeChartModal(event)">
  <div class="chart-modal">
    <div class="chart-modal-header">
      <span class="chart-modal-title" id="chart-modal-title">Chart</span>
      <button class="chart-modal-close" onclick="closeChartModal(null)" title="Close">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M18 6L6 18M6 6l12 12"/>
        </svg>
      </button>
    </div>
    <div class="chart-modal-body">
      <iframe id="chart-modal-iframe" src="" title="Chart fullscreen"></iframe>
    </div>
  </div>
</div>
















----------------------------------------




function openChartModal(chartUrl, title) {
  const overlay = document.getElementById('chart-modal-overlay');
  document.getElementById('chart-modal-iframe').src = chartUrl;
  document.getElementById('chart-modal-title').textContent = title || 'Chart';
  overlay.classList.add('open');
  document.body.style.overflow = 'hidden';
}

function closeChartModal(event) {
  // Close only if clicking the overlay backdrop (not the modal itself)
  if (event && event.target !== document.getElementById('chart-modal-overlay')) return;
  const overlay = document.getElementById('chart-modal-overlay');
  overlay.classList.remove('open');
  document.getElementById('chart-modal-iframe').src = '';
  document.body.style.overflow = '';
}

// Close on Escape key
document.addEventListener('keydown', e => {
  if (e.key === 'Escape') closeChartModal(null);
});












-----------------------------


onclick="openChartModal('${API_BASE}/chart/${encodeURIComponent(chartFile)}', '${(title||'Chart').replace(/'/g,"\\'")}')">

